---
title: 静态网站服务
description: MetaBase 内置的静态网站生成器，支持 Markdown 文档、博客、官网等静态网站。
order: 15
section: getting-started
tags: [www, static-site, markdown, documentation]
category: docs
---

# 静态网站服务

MetaBase 内置了强大的静态网站生成器，专为技术文档、博客、官网等场景设计。支持 Markdown 自动转换、Front Matter 元数据、自动导航生成等功能。

## 🚀 核心特性

### Markdown 支持
- **自动转换**: 实时将 Markdown 转换为 HTML
- **Front Matter**: 支持 YAML 元数据配置
- **扩展语法**: GitHub Flavored Markdown (GFM)、表格、删除线、链接自动识别
- **代码高亮**: 内置代码块语法高亮

### 导航系统
- **自动生成**: 扫描文档目录自动生成导航菜单
- **分组管理**: 按 section 字段自动分组
- **排序支持**: 通过 order 字段控制显示顺序
- **活动状态**: 自动高亮当前页面

### 模板系统
- **响应式设计**: 移动端友好的界面布局
- **搜索功能**: 内置文档搜索（可扩展）
- **主题支持**: 现代化的 UI 设计
- **自定义配置**: 支持自定义样式和脚本

## 📖 使用方法

### 启动服务

```bash
# 默认启动静态网站服务 (端口 7609)
./bin/metabase

# 或者显式指定启动 www 服务
./bin/metabase www serve

# 自定义端口和主机
./bin/metabase www serve --port 8080 --host 0.0.0.0

# 开发模式（启用热重载）
./bin/metabase www serve --dev
```

### 构建静态文件

```bash
# 构建静态网站到 dist 目录
./bin/metabase www build

# 指定输出目录
./bin/metabase www build --output public

# 使用配置文件
./bin/metabase www serve --config config.yaml
```

## 📁 目录结构

```
project/
├── docs/              # Markdown 文档目录
│   ├── overview.md    # 首页文档
│   ├── guide.md       # 使用指南
│   └── api.md         # API 文档
├── web/               # 静态资源目录
│   ├── assets/        # CSS、JS、图片等资源
│   ├── templates/     # 自定义模板
│   └── index.html     # 首页模板
└── config.yaml        # 配置文件（可选）
```

## ⚙️ Front Matter 配置

每个 Markdown 文件都可以包含 Front Matter 元数据：

```yaml
---
title: 文档标题          # 文档标题（必填）
description: 文档描述    # 文档描述
order: 10              # 导航排序（数字越小越靠前）
section: getting-started  # 分组名称
tags: [tag1, tag2]     # 标签
category: docs         # 分类
---
```

### Section 分组

预定义的分组名称：

- `getting-started`: 开始使用
- `core-concepts`: 核心概念
- `api`: API 参考
- `deployment`: 部署
- 其他自定义分组

## 🎨 自定义配置

### 配置文件

创建 `config.yaml` 文件来自定义配置：

```yaml
server:
  host: "localhost"
  port: 7609
  dev_mode: true

paths:
  root_dir: "docs"
  template_dir: "templates"
  asset_dir: "assets"

site:
  title: "My Documentation"
  subtitle: "技术文档站点"
  description: "基于 MetaBase 的文档站点"
```

### 自定义样式

在 `assets/` 目录下放置自定义 CSS 和 JS 文件：

- `assets/docs.css`: 主要样式文件
- `assets/docs.js`: 交互脚本
- `assets/custom.css`: 自定义样式
- `assets/custom.js`: 自定义脚本

## 🛠️ 开发模式

开发模式提供以下功能：

- **热重载**: 文件修改自动刷新
- **错误提示**: 详细的错误信息
- **调试日志**: 开发调试信息
- **性能监控**: 页面加载性能统计

```bash
# 启动开发模式
make dev
# 或
./bin/metabase www serve --dev
```

## 📝 示例文档

### 基本 Markdown 文档

```markdown
---
title: 快速开始
description: 如何快速使用 MetaBase 静态网站服务
order: 20
section: getting-started
---

# 快速开始

这是一个快速开始指南。

## 功能特性

- 特性一
- 特性二

### 代码示例

```go
package main

func main() {
    fmt.Println("Hello, MetaBase!")
}
```

### 表格示例

| 功能 | 说明 | 状态 |
|------|------|------|
| Markdown | 自动转换 | ✅ |
| 导航 | 自动生成 | ✅ |
| 搜索 | 内置支持 | 🚧 |
```

## 🔧 高级功能

### 模板自定义

支持自定义 HTML 模板：

1. 在 `templates/` 目录下创建模板文件
2. 使用 Go template 语法
3. 支持变量和函数调用

### 插件扩展

支持通过 JavaScript 插件扩展功能：

- 搜索插件
- 主题切换
- 评论系统
- 分析统计

### 多语言支持

支持多语言文档站点：

- 按语言分目录组织
- 自动语言切换
- 国际化支持

## 📊 性能优化

- **缓存机制**: 智能缓存提升性能
- **压缩优化**: 自动压缩静态资源
- **CDN 友好**: 支持静态资源 CDN 部署
- **SEO 优化**: 自动生成 meta 标签

## 🚀 部署方案

### 直接部署

```bash
# 构建静态文件
./bin/metabase www build --output dist

# 部署到服务器
rsync -av dist/ user@server:/var/www/html/
```

### Docker 部署

```dockerfile
FROM alpine:latest
RUN apk add --no-cache ca-certificates
COPY metabase /usr/local/bin/
COPY docs /app/docs
COPY web /app/web
WORKDIR /app
EXPOSE 7609
CMD ["metabase"]
```

### CDN 部署

构建后的静态文件可以部署到任何静态托管服务：

- GitHub Pages
- Netlify
- Vercel
- AWS S3
- 阿里云 OSS

## 🔗 相关链接

- [架构文档](/docs/architecture) - 了解技术架构
- [配置文档](/docs/config) - 详细配置说明
- [部署文档](/docs/deploy) - 部署指南
- [API 文档](/docs/api) - REST API 参考